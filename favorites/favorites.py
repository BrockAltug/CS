#cd ./favorites --opens the favorites directory
#sqlite3 favorites.db --access SQL database
#schema --view available tables

#UPDATE shows SET title = 'Adventure Time' WHERE title LIKE 'adventure time%'; --updates the show name if there are any similarities LIKE
#SELECT DISTINCT title FROM shows; --shows unique titles from the shows table